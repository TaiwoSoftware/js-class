# Javascript class from a documentation concerning javascript
### Client Side:
This part is talking about the part of javascript where we handle the event the user execute on the page.This is how users interact with the page via HTML.


#Syntax
Before you get the syntax for javascript you need to have a very Standard understanding of HTML so the syntax for javascript is two ways you can either embed it in your html file or you create a new javascript file so to embed it you add the script tag first a < then script in it 